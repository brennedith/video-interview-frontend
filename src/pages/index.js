import React from 'react';

import FullHeight from './components/FullHeight';

const index = () => {
  return (
    <FullHeight centered>
      <a href="/login">
        <h1 className="title is-1 has-text-link">Video Interview</h1>
      </a>
    </FullHeight>
  );
};

export default index;
